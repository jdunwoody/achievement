require 'spec_helper'

describe SkillTree do

  before :each do
    #subject.stub!(:property).and_return(@property)
  end

  context "validations" do

    describe 'api source offer id' do
      #it 'should not be validated if api source is nil' do
        #@property.stub!(:api_source).and_return(nil)
        #subject.should_not validate_presence_of(:api_source_offer_id)
      #end
    end
  end
end
